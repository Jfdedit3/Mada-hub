-- Regroupement des variables locales
local NotificationLoad = loadstring(game:HttpGet('https://raw.githubusercontent.com/treeofplant/Notif/main/library.lua', true))()

if not NotificationLoad then
    error("Notification library failed to load.")
end

local ScreenGui = Instance.new("ScreenGui")
local MainFrame = Instance.new("Frame")
local TitleLabel = Instance.new("TextLabel")
local CloseButton = Instance.new("TextButton")
local ButtonContainer = Instance.new("Frame")
local ToggleVisibilityButton = Instance.new("TextButton")
local FooterLabel = Instance.new("TextLabel")

-- Fonction de notification
local function createNotification(title, description, timeout)
    NotificationLoad:NewNotification({
        ["Mode"] = "Custom",
        ["Title"] = title,
        ["Description"] = description,
        ["Notification_Image"] = "rbxassetid://",
        ["Notification_Sound"] = "rbxassetid://6031098704",
        ["Timeout"] = timeout or 5,
        ["Audio"] = true
    })
end

-- Fonction pour appliquer des coins arrondis
local function applyCorners(instance, cornerRadius)
    local corner = Instance.new("UICorner")
    corner.CornerRadius = UDim.new(0, cornerRadius)
    corner.Parent = instance
end

-- Créer l'interface utilisateur
ScreenGui.Name = "MyScreenGui"
ScreenGui.Parent = game.CoreGui

MainFrame.Name = "MainFrame"
MainFrame.Parent = ScreenGui
MainFrame.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
MainFrame.Position = UDim2.new(0.5, -200, 0.5, -150)
MainFrame.Size = UDim2.new(0, 400, 0, 300)
MainFrame.BackgroundTransparency = 0.1
applyCorners(MainFrame, 12)

TitleLabel.Name = "TitleLabel"
TitleLabel.Parent = MainFrame
TitleLabel.BackgroundColor3 = Color3.fromRGB(30, 30, 30)
TitleLabel.Size = UDim2.new(1, 0, 0, 30)
TitleLabel.Font = Enum.Font.GothamBold
TitleLabel.Text = "Mada hub v3"
TitleLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TitleLabel.TextSize = 16
applyCorners(TitleLabel, 10)

CloseButton.Name = "CloseButton"
CloseButton.Parent = MainFrame
CloseButton.BackgroundColor3 = Color3.fromRGB(255, 50, 50)
CloseButton.Position = UDim2.new(0.95, -20, 0, 5)
CloseButton.Size = UDim2.new(0, 20, 0, 20)
CloseButton.Font = Enum.Font.GothamBold
CloseButton.Text = "X"
CloseButton.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseButton.TextSize = 14
applyCorners(CloseButton, 5)

CloseButton.MouseButton1Click:Connect(function()
    ScreenGui:Destroy()
end)

ButtonContainer.Name = "ButtonContainer"
ButtonContainer.Parent = MainFrame
ButtonContainer.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
ButtonContainer.Position = UDim2.new(0, 0, 0, 30)
ButtonContainer.Size = UDim2.new(1, 0, 1, -70) -- Ajuster la hauteur pour laisser de la place au footer
applyCorners(ButtonContainer, 10)

-- Tableau des boutons
local buttons = {
    {text = "KOLINSKI admin", url = "https://raw.githubusercontent.com/Jfdedit3/KOLINSKI/refs/heads/main/KOLINSKI.lua"},
    {text = "Cmd Fe", url = "https://raw.githubusercontent.com/lxte/cmd/main/testing-main.lua"},
    {text = "Honoka Executor", url = "https://raw.githubusercontent.com/Jfdedit3/honoka-executor/main/Honoka%20executor%20byz4trox"},
    {text = "Nameless Hub", url = "https://raw.githubusercontent.com/Jfdedit3/Nameless-hub-/refs/heads/main/Nameless%20musique"},
    {text = "Velocity Ware", url = "https://raw.githubusercontent.com/ImNotRox1/VWare/main/Universal.lua"}
}

-- Fonction pour créer un bouton stylisé (taille 2 fois plus petite)
local function createButton(text, url, position)
    local button = Instance.new("TextButton")
    button.Name = text
    button.Parent = ButtonContainer
    button.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    button.Position = UDim2.new(0, 10, 0, position)
    button.Size = UDim2.new(1, -20, 0, 20)  -- Réduire la hauteur du bouton à 20
    button.Font = Enum.Font.Gotham
    button.Text = text
    button.TextColor3 = Color3.fromRGB(255, 255, 255)
    button.TextSize = 14
    applyCorners(button, 8)

    button.MouseButton1Click:Connect(function()
        createNotification(text, text .. " is executing.", 3)

        if not url:match("^https://raw.githubusercontent.com/") then
            createNotification("Error", "Invalid script URL.", 5)
            return
        end

        local success, err = pcall(function()
            local startTime = tick() -- Début de l'exécution
            local scriptCode = game:HttpGet(url, true)
            if not scriptCode then
                error("Failed to download script.")
            end
            loadstring(scriptCode)()
            local endTime = tick() -- Fin de l'exécution
            local executionTime = endTime - startTime -- Temps d'exécution

            createNotification("Success", text .. " executed in " .. string.format("%.2f", executionTime) .. " seconds.", 5)
        end)

        if not success then
            createNotification("Error", "Failed to execute: " .. err, 5)
        end
    end)

    button.MouseEnter:Connect(function()
        button.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    end)
    button.MouseLeave:Connect(function()
        button.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
    end)
end

-- Créer les boutons dynamiquement à partir du tableau, avec un espacement adapté
for index, buttonInfo in ipairs(buttons) do
    createButton(buttonInfo.text, buttonInfo.url, (index - 1) * 25)  -- Réduire l'espacement entre les boutons
end

-- Ajouter le texte de footer
FooterLabel.Name = "FooterLabel"
FooterLabel.Parent = MainFrame
FooterLabel.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
FooterLabel.Position = UDim2.new(0, 10, 1, -30) -- Position en bas du MainFrame
FooterLabel.Size = UDim2.new(1, -20, 0, 30)
FooterLabel.Font = Enum.Font.GothamBold
FooterLabel.Text = "z4trox - Version 3.0 | https://discord.gg/a8DhGKRNEq"
FooterLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
FooterLabel.TextSize = 12
applyCorners(FooterLabel, 8)

ToggleVisibilityButton.Name = "ToggleVisibilityButton"
ToggleVisibilityButton.Parent = ScreenGui
ToggleVisibilityButton.BackgroundColor3 = Color3.fromRGB(40, 40, 40)
ToggleVisibilityButton.Position = UDim2.new(0.5, -50, 0.35, -150)
ToggleVisibilityButton.Size = UDim2.new(0, 135, 0, 30)
ToggleVisibilityButton.Font = Enum.Font.Gotham
ToggleVisibilityButton.Text = "Toggle UI"
ToggleVisibilityButton.TextColor3 = Color3.fromRGB(255, 255, 255)
ToggleVisibilityButton.TextSize = 14
applyCorners(ToggleVisibilityButton, 10)

local function toggleMainFrame()
    MainFrame.Visible = not MainFrame.Visible
end

ToggleVisibilityButton.MouseButton1Click:Connect(toggleMainFrame)

createNotification("HELLO!", "Welcome to Mada hub v3", 5)
